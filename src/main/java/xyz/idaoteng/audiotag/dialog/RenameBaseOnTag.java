package xyz.idaoteng.audiotag.dialog;

import javafx.scene.layout.VBox;
import javafx.stage.Stage;

public class RenameBaseOnTag {
    private static final Stage stage = new Stage();

    static {
        stage.setTitle("基于标签重命名");

        VBox vBox = new VBox();

    }
}
